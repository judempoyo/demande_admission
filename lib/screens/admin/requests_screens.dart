import 'package:flutter/material.dart';

class RequestsScreens extends StatefulWidget {
  const RequestsScreens({super.key});

  @override
  State<RequestsScreens> createState() => _RequestsScreensState();
}

class _RequestsScreensState extends State<RequestsScreens> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('')), body: Container());
  }
}
